# Firebase browser API keys are intentionally public client-side identifiers.
# Security is enforced by Firestore Security Rules and Firebase Auth Authorized Domains
# in the Firebase console — not by hiding this config. See:
# https://firebase.google.com/docs/projects/api-keys
paths-ignore:
  - "index.html"
